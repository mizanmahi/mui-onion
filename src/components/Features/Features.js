import { Box, Container } from '@mui/material';
import React from 'react';

const Features = () => {
   return (
      <Box
         sx={{
            width: '100%',
            my: 5,
         }}
      >
         <Container>
            Why choose us
         </Container>
      </Box>
   );
};

export default Features;
